const $=(s,c=document)=>c.querySelector(s);const $$=(s,c=document)=>[...c.querySelectorAll(s)];
const nav=$('.nav'),toggle=$('.menu-toggle');toggle?.addEventListener('click',()=>{nav.classList.toggle('open');toggle.setAttribute('aria-expanded',nav.classList.contains('open'))});
const page=location.pathname.split('/').pop()||'index.html';$$('.nav a').forEach(a=>{if(a.getAttribute('href')===page)a.classList.add('active')});
addEventListener('scroll',()=>{document.documentElement.style.setProperty('--progress',`${scrollY/(document.body.scrollHeight-innerHeight)*100}%`)},{passive:true});
$('.top')?.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});$$('.reveal,.service-card,.project-card').forEach(el=>io.observe(el));
const countIO=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting||e.target.dataset.done)return;e.target.dataset.done=1;const end=+e.target.dataset.count;let n=0;const step=()=>{n+=Math.ceil(end/42);e.target.textContent=Math.min(n,end)+'+';if(n<end)requestAnimationFrame(step)};step()}),{threshold:.6});$$('[data-count]').forEach(el=>countIO.observe(el));
$$('.compare').forEach(box=>{const input=$('input',box);const set=()=>{box.style.setProperty('--split',input.value+'%');$('.after',box).style.clipPath=`inset(0 0 0 ${input.value}%)`};input.addEventListener('input',set);set()});
$$('.faq button').forEach(btn=>btn.addEventListener('click',()=>{const open=btn.classList.toggle('open');$('span',btn).textContent=open?'−':'+'}));
$$('[data-slider]').forEach(slider=>{const quotes=$$('.quote',slider),dots=$('.dots',slider);let i=0;quotes.forEach((_,idx)=>{const b=document.createElement('button');b.setAttribute('aria-label',`Show testimonial ${idx+1}`);b.onclick=()=>show(idx);dots.appendChild(b)});function show(n){quotes[i].classList.remove('active');dots.children[i].classList.remove('active');i=n;quotes[i].classList.add('active');dots.children[i].classList.add('active')}show(0);setInterval(()=>show((i+1)%quotes.length),4500)});
$$('[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{const f=btn.dataset.filter;$$('[data-filter]').forEach(b=>b.classList.remove('active'));btn.classList.add('active');$$('[data-category]').forEach(card=>{card.style.display=f==='all'||card.dataset.category.includes(f)?'block':'none'})}));
$$('.form,.mini-form').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();form.reset();const msg=form.nextElementSibling;if(msg?.classList.contains('success'))msg.classList.add('show');else alert('Thank you. Lumora will contact you shortly.')}));
